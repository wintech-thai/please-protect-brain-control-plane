apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gitea-secret
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: k8s-secret-store      # name of the SecretStore (or kind specified)
  target:
    name: gitea-secret  # name of the k8s Secret to be created
  data:
  - secretKey: GIT_USER
    remoteRef:
      key: initial-secret
      property: GIT_USER

  - secretKey: GIT_PASSWORD
    remoteRef:
      key: initial-secret
      property: GIT_PASSWORD
