apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pp-web-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
    nginx.org/client-max-body-size: 50m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.domainPpWeb }}
      #secretName: pp-web-cert
  rules:
    - host: {{ .Values.domainPpWeb }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pp-{{ .Values.envGroup }}-pp-web
                port:
                  number: 80
